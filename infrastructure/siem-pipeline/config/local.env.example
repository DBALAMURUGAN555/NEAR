# IC Custody Platform - Local Environment Configuration
# Copy this file to local.env and update with your actual credentials

# Elasticsearch Configuration
ELASTIC_PASSWORD=SecureElasticPassword123!
KIBANA_PASSWORD=SecureKibanaPassword123!
KIBANA_ENCRYPTION_KEY=a-32-character-long-encryption-key-here

# Redis Configuration
REDIS_PASSWORD=SecureRedisPassword123!

# Grafana Configuration
GRAFANA_USER=admin
GRAFANA_PASSWORD=SecureGrafanaPassword123!

# PagerDuty Integration (replace with your actual keys)
PAGERDUTY_INTEGRATION_KEY=your-pagerduty-integration-key
PAGERDUTY_CRITICAL_KEY=your-pagerduty-critical-key
PAGERDUTY_HIGH_KEY=your-pagerduty-high-key
PAGERDUTY_SECURITY_KEY=your-pagerduty-security-key

# Slack Integration (replace with your actual webhook URL)
SLACK_API_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# SMTP Configuration for email alerts
SMTP_HOST=localhost:587
SMTP_FROM=alerts@custody-platform.com
SMTP_USERNAME=
SMTP_PASSWORD=

# Webhook Configuration
WEBHOOK_SECRET=SecureWebhookSecret123!

# Service URLs (for production environments, replace with actual URLs)
KIBANA_URL=http://localhost:5601
GRAFANA_URL=http://localhost:3000
ALERTMANAGER_URL=http://localhost:9093
ADMIN_PORTAL_URL=http://localhost:8000

# Email addresses for notifications
DEFAULT_EMAIL=ops@custody-platform.com
COMPLIANCE_EMAIL=compliance@custody-platform.com

# IC Network Configuration
IC_NETWORK=local
IC_GATEWAY_URL=http://localhost:4943
EVENTS_CANISTER_ID=rdmx6-jaaaa-aaaaa-aaadq-cai

# Production Environment Variables (only for production deployments)
# ELASTIC_CLOUD_ID=your-elastic-cloud-id
# ELASTIC_CLOUD_AUTH=your-elastic-cloud-auth
# KIBANA_CLOUD_URL=https://your-deployment.kb.us-central1.gcp.cloud.es.io:9243
# GRAFANA_CLOUD_URL=https://your-stack.grafana.net
# SSL_CERT_PATH=/path/to/ssl/cert.pem
# SSL_KEY_PATH=/path/to/ssl/key.pem
